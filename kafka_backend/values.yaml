# values.yaml
# Default values
module: "generic"
environment: "default"

# Topics configuration
topics:
  - name: "test-topic"
    partitions: 1
    replicationFactor: 1
    module: "kafka"
  - name: "generic-topic"
    partitions: 3
    replicationFactor: 1
    module: "kafka"

# Server configuration
server:
  file: "server.properties"